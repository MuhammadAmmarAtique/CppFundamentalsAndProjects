#include <iostream>

using namespace std;

int main()

{

   int arr[4];
   cout<<"put values";
   cin>>arr[0]>>arr[1]>>arr[2]>>arr[3];
      //index=0,index=1,index=2,index=3   (a.index is used to access the elements of array)
                                      //  (b.Also it is used to get values of arrays element from user_)
   cout<<"lets see"<<endl;
   cout<<arr[3];

return 0;

}